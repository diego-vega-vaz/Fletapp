# Copia este archivo como .env y llena los valores.
# El .env real NUNCA se sube: esta en .gitignore.
#
# Estas dos las encuentras en Supabase > Project Settings > API.
# La anon key es publica por diseno: la seguridad la da RLS, no el secreto.
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key
